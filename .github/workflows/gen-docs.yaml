name: Generate element and trigraph docs

on:
  push:

jobs:
  gen-docs:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'adopt'
    - name: Build jar
      run: sbt vyxalJVM/assembly
    - name: Update elements.txt and trigraphs.txt
      run: ./mill jvm.docs
    - name: Update nanorc files for JLine
      run: ./mill jvm.nanorc
    - name: Stage changes
      run: git add .
    - name: Commit and push
      uses: stefanzweifel/git-auto-commit-action@v4
      with:
        commit_message: "Refresh element and/or trigraph docs from recent commit"

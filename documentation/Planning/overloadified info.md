F, G, H = monadic functions
f, g, h = dyadic functions
ḟ, ġ, ḣ = any arity

Syntax

- [x] λ   NA  =   Lambda opener
- [x] ƛ   NA  =   Mapping lambda opener
- [x] Λ   NA  =   Filtering lambda opener
- [x] ʎ   NA  =   Reduction lambda opener
- [x] ỿ   NA  =   Sorting lambda opener
- [x] ξ   NA  =   Lambda with arity -1
- [ ] ⍾   NA  =   Eager map opener
- [ ] ⎋  NA  =   Close structure and get first item
- [ ] ⍟ NA  = Close structure and flatten


Modifiers

- [x] ∥   F<any>, G<any>  =   Parallel apply. Execute F and G on two different stacks, pop the arguments G used, and push the result of F then G.
- [x] ∦   F<any>, G<any>  =   Parallel apply and wrap. Equivalent to ∥FG;
- [x] ∺   F<1>, G<1>  = Correspond. Given stack of ... x y, results in ... G(y) F(x)
- [x]     F<2+>, G<2+>    =   Given stack of ... a b c d, results in ... G(d, c) F(b, a)
- [ ] ⁜   F<1>    =   Group By Element. Group items in the top of stack by application of function F
- [ ]     F<2+>   = Window Reduce. Reduce overlapping (arity) windows by function F.
- [ ] ⑴   NA  =   Next element as lambda
- [ ] ⑵   NA  =   Next 2 elements as lambda
- [ ] ⑶   NA  =   Next 3 elements as lambda
- [ ] ⑷   NA  =   Next 4 elements as lambda
- [ ] ⎂   F<any>  =   Both. Apply F to both the top of stack (or however many arguments), and under stack (or however many arguments under the arity). Effectively ... F(top - arity, top - arity * 2) F(top -> top - arity)
- [ ] 🜳   F<2+>, G<2+> = G(F(x, y), y)
- [ ] ▥   F<1>    =   Map over columns. To each column of top, apply F.
- [ ]     F<2+>   =   Reduce over columns. To each column of top, reduce by F.
- [ ] ▦   F<2>    =   Outer Product. [F(under, x) for x in top]
- [ ] ¨   F<1>    =   Map. Apply F to each item in top
- [ ]     F<2+>    =   Zip with. Apply F to each item in zip(top, under, ...[kicker, ...])


Elements

Types = num (number) str (string) lst (list) fun (function) any (any value) qnt (quantum value)
* after a pair of types indicates type switching occurs
! after a pair of types inicates depth switching (deeper on left, shallower on right)
A lack of a lst overload indicates auto-vectorisation.

a = lhs
b = rhs

- [x] ⊞ (any) = [a.count(_) for _ in a]
- [ ] ζ () = When entering a lambda, store the popped value in this
- [ ] γ () = When entering a lambda, retrieve top of outer stack
- [ ] ⎊ (any, fun) = map over permutations
- [x] ÷ (num, num) = a divided by b
- [x]   (str, num)* = split string b into chunks of size a
- [x]   (str, str) = split a on regex b
- [x] × (num, num) = a times b
- [x]   (str, num)* = repeat string a, b times
- [x]   (str, str) = ring translate
- [x] ∧ (any, any) = b && a (first falsey)
- [x] ∨ (any, any) = b || a (first truthy)
- [x] ¬ (any) = not a (truthy => 0, falsey => 1)
- [x] ʀ (num) = range [0, a]
- [x]   (str) = lowercase
- [x] ʁ (num) = range [0, a)
- [x]   (str) = uppercase
- [x] ɾ (num) = range [1, a]
- [x]   (str) = is character a an alphabetical (a-zA-Z) letter?
- [x] ‹ (num) = a - 1
- [x]   (str) = Pad string with 0s to length nearest multiple of 8
- [x] › (num) = a + 1
- [x]   (str) = Replace spaces with 0s
- [x] ! (num) = Factorial
- [x]   (str) = Titlecase
- [x] $ (any, any) = ...d c b a => d c a b
- [x] % (num, num) = a modulo b
- [x]   (str, any) = a.format(b) # format on %s
- [x] & (any, any) = append b to a
- [x] * (num, num) = a to the power of b
- [x] + (num, num) = a + b
- [x]   (str, num)* = str(a) + str(b)
- [x]   (str, str) = a + b
- [x] , (any) = Println
- [x] - (num, num) = a - b
- [x]   (str, num) = append b "-"s to a
- [x]   (num, str) = prepend a "-"s to b
- [x]   (str, str) = remove all instance of regex b in a
- [x] : (any) = ...d c b a => d c b a a
- [x] ; (any, any) = [a, b]
- [x] < (num, num) = a < b
- [x]   (str, num)* = str(a) < str(b)
- [x]   (str, str) = a < b (lexiographically)
- [x] = (num, num) = a == b
- [x]   (str, num)* = str(a) == str(b)
- [x]   (str, str) = a == b
- [x] > (num, num) = a > b
- [x]   (str, num)* = str(a) > str(b)
- [x]   (str, str) = a > b (lexiographically)
- [x] ? = push input eval'd
- [x] @ (num, num) = |a - b|
- [x]   (str, str) = Levenshtein distance between a and b
- [x]   (any, fun)* = Reduce overlaps in list a by function b
- [x] A (num) = Are all digits of a truthy?
- [x]   (str) = Is character a vowel?
- [x]   (lst) = Are all items in a truthy?
- [x] B (num) = Convert a to binary (list of digits)
- [x]   (str) = each character in a to the binary representation of its character code
- [x] C (any, any)! = count of a in b
- [x] D (any) = ...d c b a => d c b a a a
- [x] E (num) = a ** 2
- [x]   (str) = Evaluate str (think python eval)
- [x] F (any, fun) = a.filter(b)
- [x]   (any, non-fun)! = a.find(b)
- [x] G (lst) = max(a)
- [x]   (scl, scl) = max(a, b)
- [x] H (num) = to_hex(a) (base 10 -> base 16)
- [x]   (str) = from_hex(a) (base 16 -> base 10)
- [x] I (any, fun)* = a.filter(x => !b(x)) (reject by)
- [x]   (any, non-fun) = Interleave
- [x] J (lst, lst) = a.addAll(b) (merge)
- [x]   (num, num) = [a, b]
- [x]   (num, str) = a.toString + b
- [x]   (str, str) = a + b
- [x] K (num) = factors of a
- [x]   (str) = is string numeric
- [x] L (any) = length
- [x] M (any, fun)* = a.map(b)
- [x]   (lst, lst) = mold a to shape of b
- [x]   (num, num) = multiplicity
- [x]   (lst, num)* = vectorised num,num
- [x] N (num) = -a
- [x]   (str) = swap case
- [x]   (fun) = First non-negative integer x where a(x) is true
- [x] O (num) = character with corresponding character code
- [x]   (str) = number(s) that correspond to character codes
- [x] P (any) = Prefixes
- [x] Q (any, num) = remove bth item of a 
- [x]   (str, str) = Get all groups in a regex match
- [x] R (any, fun)* = a.reduce(b) (makes b have arity 2)
- [x]   (num, num) = range(a, b) ([a, b))
- [x]   (str, str) = Is there a regex match of pattern b in string a 
- [x] S (any) = sort in ascending order
- [x] T (lst) = Transpose
- [x]   (num) = Triple
- [x] U (any) = Uninterleave
- [x] V (lst) = [x[::-1] for x in a]
- [x] W (...) = ...d c b a => [...d c b a]
- [x] X (any, any) = Cartesian product
- [x] Y (any, num) = [a].repeat(b)
- [x] Z (any, any) = zip(a, b)
- [x] ^ (...) = ...d c b a => a b c d ...
- [x] _ (any) = pop / discard
- [x] a (num) = any digit non-0?
- [x]   (str) = is character uppercase?
- [x]   (lst) = are any items truthy?
- [x] b (any) = convert from binary
- [x] c (any, any)! = a in b?
- [x] d (any) = a + a 
- [x] e (num) = even?
- [x]   (str) = split on newlines
- [x] f (any) = Flatten
- [x] g (lst) = min(a)
- [x]   (scl, scl) = min(a, b)
- [x] h (any) = a[0]
- [x] i (any, num)* (except num, num) = a[b]
- [x]   (lst, lst) = [a[x] for x in b]
- [x]   (str, str) -> enclose b in a (a[0:len(a)//2] + b + a[len(a)//2:])
- [x]   (any, fun)* = Apply b on a and collect unique values. Does include the initial value.
- [x] j (any, any) = join a on b
- [x] l (num, num) = Logarithm
- [x]   (str, str) = len(a) == len(b)
- [x]   (str, num)* = len(a) == b
- [x]   (any, fun)* = apply until a previous value is repeated, collecting intermediate results
- [x] m () = context variable m
- [x] n () = context variable n
- [x] o (any, num) = Overlapping slices of a of length b
- [x]   (any) = Overlapping slices of a of length 2
- [x] p (any, any) = Prepend
- [x] q (any) = Quotify
- [x] r (any, any, any) = s/b/c
- [x] s (any, any) = split a on b
- [x] t (any) = a[-1]
- [x] u (any) = uniquify
- [ ] v # TODO
- [x] w (any) = [a]
- [ ] x () = call the current function (or top level program)
- [x] y (any, any, any) = Transliterate
- [x] z (any, any) = Transpose with filler (the thing jelly has)
- [x] ⨥ (num) = a + 2
- [x]   (str) = string length == 1
- [x] ⨪ (num) = a - 2
- [ ]   (str) = tbd (not append two "-"s though that'd be silly even by my standards)
- [x] Σ (any) = sum of a 
- [x]   (lst[at least one string]) = join on nothing and eval
- [x] Π (any) = product of a
- [x] σ (any) = Cumulative sums of a
- [x] ⇧ (any) = Grade up
- [x] ⇩ (any) = Grade down
- [x] ∪ (any, any) = Set union
- [x] ∩ (any, any) = Set intersection
- [x] ⊍ (any, any) = set xor
- [x] ⦰ (any, any) = set difference
- [x] « (num, num) = left bit Shift
- [x]   (str, num)* = a padded to length b with spaces prepended
- [x]   (str, str) = a padded to length of b with spaces prepended
- [x] » (num, num) = right bit shift
- [x]   (str, num) = a padded to length b with spaces appended
- [x]   (str, str) = a padded to length of b with spaces appended
- [x] Ɠ (any) = max(a), but don't pop
- [x] ɠ (any) = min(a), but don't pop
- [x] Ġ (lst, num)* = [max(x, b) for x in a]
- [x]   (lst, lst) = [max(x, y) for (x, y) in zip(a, b)]
- [x] ġ (lst, num)* = [min(x, b) for x in a]
- [x]   (lst, lst) = [min(x, y) for (x, y) in zip(a, b)]
- [x] ⌈ (num) = Ceiling
- [x]   (str) = split on spaces
- [x] ⌊ (num) = Floor
- [x]   (str) = cast to int, 0 if no numeric characters
- [x] ⊖ (any, num)* = a[0:b]
- [x] ⌽ (any, num)* = a[1:b]
- [x] £ (any) = set Register
- [x] ¥ () = push Register
- [ ] ▲ # TODO
- [x] ↜ (...) = rotate stack left
- [x] ↝ (...) = rotate stack right
- [x] ⬳ (non-num) = rotate item left
- [x]     (any, num) = rotate item left b times
- [x] ⟿ (non-num) = rotate item right
- [x]    (any, num) = rotate item right b times
- [x] ≜ (any, num, non-fun) = a[b] = c
- [x]    (any, num, fun) = a[b] = c(a[b])
- [x]    (any, lst[num], fun) = essentially vectorised of (any, num, fyn)
- [x]    (lst, lst, lst) = [a[x] = y for (x, y) in zip(x, y)]
- [x]    (str, str, str) = replace regex matches of pattern b in string a with c
- [x]    (str, str, fun)* = replace regex matches of pattern b in string a with the result of applying c to each match
- [x]    (rec, str, str)* = a.b = c
- [x] ⎀ (any, num, any) = insert c at position b in a
- [x]    (any, lst, any) = insert c at positions b in a
- [x]    (any, lst[num], lst) = insert c[i] at position b[i] in a
- [x] ◲ (any) = sublists of a
- [x] ⊢ (num, num) = a (base 10) in base b
- [x]   (num, str|lst) = a in base with alphabet b
- [x]   (lst, num) = each x in a in base b
- [x]   (lst, lst) = each x in a in base with alphabet b
- [x]   (str, str) = All matches of b in a
- [x] ⊣ (num, num) = a (in base b) in base 10
- [x]   (num, str|lst) = a from base with alphabet b to base 10
- [x]   (lst, num) = this, but vectorised
- [x] ɦ (any) = a[0], but don't pop
- [x] ʈ (any) = a[-1], but don't pop
- [x] ᐐ (any) = a[:-1] (because the : is on the left)
- [x] ᐵ (any, num) = a[b:]
- [x] ᐕ (any) = a[1:] (because the : is on the right)
- [x] ½ (num) = a / 2
- [x]   (str) = a split into 2 parts with roughly equal length
- [x] ƶ (any) = [0, len(x))
- [x] Ƶ (any) = [1, len(x)]
- [ ] ⁰ () = first input
- [ ] ¹ () = second input
- [ ] ² (num) = Square
- [ ]   (str) = a split into pairs
- [ ] ³ (num) = cube
- [ ]   (str) = a split into chunks of length 3
- [ ] ⅟ (num) = 1 / x
- [ ]   (str) = a with all whitespace removed
- [ ] ※ (any) = group by consecutive values
- [ ]    (any, fun) = group by function (keep order)
- [ ] ⇄ (any) = a[::-1] reverse
- [ ] ⧖ (any) = permutations
- [ ] ‰ (num, num) = [a // b, a % b]
- [ ] ≛ (num, num) = does b divide a?
- [ ]   (str, num)* = a + ' ' * b
- [ ]   (lst, fun)* = Remove duplicates from a by applying b to each element
- [ ]   (str, str) = span of first regex match of b in a
- [ ] ⁞ (any, num) = Combinations of a with length b
- [ ] ⦷ (num) = Absolute value
- [ ]    (str) = keep alphabet characters of a
- [ ] Ϣ (any, num) = a wrapped in chunks of length b
- [ ] ≤ (num, num) = a <= b
- [ ] ≥ (num, num) = a >= b
- [ ] ≠ (scl, scl) = a != b
- [ ] ≡ (any, any) = a == b (non-vec)
- [ ] • (lst, lst) = dot product
- [ ]   (num, num) = Convert a to bijective base b
- [ ]   (lst, fun) = First index of a where b is truthy
- [ ] ± (num) = sign of a
- [ ]   (str) = tbd
- [ ] † (any) = lengths of consecutive groups
- [ ] ⎙ (any) = print without popping
- [ ] ✒ (any) = print (not println)
- [ ] ≓ (num) = a + reversed(a) (as number)
- [ ]   (str) = a + reversed(a)
- [ ]   (lst) = append reversed(a) to a
- [ ] Ͼ (lst) = ¨Σ
- [ ] ⛭ (num) = a ** 10
- [ ]    (str) = Exec
- [ ]    (fun) = call function
- [ ] ⏟ (any, num) = every bth element of a
- [ ] ⌭ (num) = is a prime?
- [ ]    (str) = quote a and prepend to a
- [ ] ⏜ () = ...d c b a => ...d c b a b
- [ ] ⍢ (num) = a % 2
- [ ]    (str) = last half of a
- [ ] ℂ (num, num) = n choose K
- [ ]   (str, str) = are the character sets of a and b equal?
- [ ]   (str, num)* = all b-length combinations from a
- [ ]   (fun, any)* = run a on b until the result no longer changes returning all intermediate results
- [ ] ⌹ (lst) = List partitions of a
- [ ]    (num) = Integer partitions of a (all possible ways to sum to a)
- [ ] ⏚ (lst) = powerset
- [ ] ↯ (lst, fun) = sort by function
- [ ]   (num, num) = range(a, b+1) ([a, b])
- [ ]   (str, str) = split on regex b and keep the delimiters
- [ ] ⊠ (lst, num) = Cartesian power
- [ ] ⚅ (num) = randint(0, a)
- [ ]    (str|lst) = random element from a
- [ ] æ (any) = a, a[::-1]
- [ ] ␣ () = " " (space)
- [ ] ¶ () = "\n" (newline)
- [ ] ★ () = "*" (Asterisk)
- [ ] ᑂ (any) = a[1:], a[0]
- [ ] ∻ (num, num) = floor division
- [ ] √ (num) = sqrt(a)
- [ ] ₿ (scl) = is a truthy?
- [ ]   (lst) = vectorised
- [ ] ◌ (num) = round(x) -> round half up 
- [ ] #◌ () = list of all inputs
- [ ] δ (lst) = forward differences (⊞-)
- [ ] ☷ (lst, lst) = Partition y after truthy indices of x.
- [ ] ✇ (lst|str) = Edges. [x[0], x[-1]]
- [ ]    (num) = [real, complex]
- [ ] ⎃ (any) = Flatten x and join on nothing
- [ ] ⎶ (any, any) = trim b from both sides of a
- [ ] ⊆ (lst, lst) = is b a subset of a?
- [ ] ⍨ (any) = dump items of a
- [ ] ¤ (any) = 2Ϣ
- [ ] ⎘ (lst, num) = flatten by depth
- [ ]    (lst) = flatten one layer
- [ ] ꜝ (lst) = keep only truthy items
- [ ] ≈ (any) = all items equal?
- [ ] ≊ (any, any) = are all items in a equal to b?
- [ ] κ (num, num) = gcd(a, b)
- [ ]   (lst) = gcd(all items in a)
- [ ] ⬱ (num) = nth item from outer stack 
- [ ]    (otherwise) = top item from outer stack...?
- [ ] Þ⬱ (any, num > 0) = append a[0] until length b
- [ ]    (any, num < 0) = append a[-1] until length b
- [ ] „ (any) = join on newlines
- [ ] ” (any) = join on spaces
- [ ] “ (any) = join on nothing

Things that need to be slotted in:

- Collect while unique but doesn't include the initial value
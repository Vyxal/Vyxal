1⁽+dḞ

# 1 pushes the value 1 onto the stack
# ⁽+d pushes a dyadic function that takes two values from the stack and adds them
# Ḟ takes that function and the 1 and creates an infinite sequence based on applying the function to a stack of all previous values